# Core GUI framework
PyQt5>=5.15.9

# PDF processing library (PyMuPDF)
PyMuPDF>=1.23.3

# Image processing library
Pillow>=10.0.0

# Optional: For better threading performance
# ThreadPoolExecutor is included in Python standard library (concurrent.futures)
# No additional package needed
